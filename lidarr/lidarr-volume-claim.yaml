---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: lidarr-volume-claim
  namespace: default
spec:
  storageClassName: standard
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
